{
  "version": 2,
  "buildCommand": "npm run build:plugin",
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["design-system/**"]
      }
    }
  ]
}
